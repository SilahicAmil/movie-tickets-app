import { View } from "react-native";

const MovieDetails = ({ title, description, rating, release }) => {
  return (
    <>
      <View>{/* put Card componen stuff here and pass props  */}</View>;
    </>
  );
};

export default MovieDetails;
